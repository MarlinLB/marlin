#!/bin/sh
# Runs after removal or purge. Only daemon-reload -- dpkg has already removed
# the unit file by this point, so this just drops systemd's cached copy of it.
set -e

if [ -d /run/systemd/system ]; then
    systemctl daemon-reload >/dev/null 2>&1 || true
fi

exit 0
